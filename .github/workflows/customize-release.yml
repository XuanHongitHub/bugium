name: Customize Fork Release (Deprecated)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy repack flow
        run: |
          echo "Legacy customize-release flow is disabled."
          echo "Reason: upstream asset repack keeps Chromium branding in binary resources."
          echo "Use build.yml to publish Bugium releases and then release-manifest.yml will push to S3."
          exit 1
